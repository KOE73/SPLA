# PLAN_20260805_tools — контракт вызова инструмента

**Статус:** в работе — часть I закрыта 2026-08-05, следующая задача 4

Основание — [`IDEA_20260805_core_external-head`](../ideas/IDEA_20260805_core_external-head.md)
(разборы и обоснования там, здесь только порядок работ) и
[`IDEA_20260802_core_maf-directions`, блок 3](../ideas/IDEA_20260802_core_maf-directions.md)
(конвейер инструментов).

Порядок построен так, что **сначала идёт то, что не меняет поведение вообще**, затем извлечения с
сохранением поведения, и только в конце — то, что требует решений владельца.

---

## Как этим планом пользоваться

Это план для исполнителя, который **не участвовал в обсуждении**. Всё нужное — здесь; лезть в идеи
за обоснованием можно, но не обязательно.

**Правила, обязательные к соблюдению:**

1. **Одна задача за один заход.** Не начинать следующую, не закрыв предыдущую проверкой.
2. **Трогать только файлы из списка «можно».** Список «нельзя» — жёсткий; если кажется, что надо
   тронуть запрещённое, это условие «стоп и доложи».
3. **Стоп и доложи** — при любом расхождении с описанным: файла нет, метод другой, тест падает не
   тот. Не улучшать по своему усмотрению, не расширять объём.
4. **В задаче нет открытых вопросов.** Если вопрос возник — он не твой, доложи.
5. После задачи — отметить её в этом файле как выполненную (`— сделано ГГГГ-ММ-ДД`).

### Базовая линия (снято 2026-08-05, до начала работ)

```
dotnet build SPLA.slnx        → Ошибок: 0, Предупреждений: 0  (~25 c)
dotnet test tests/SPLA.Tests  → пройдено 548, НЕ ПРОЙДЕНО 2, всего 550  (~46 c)
```

**Два теста падают ДО всяких изменений.** Это не твоя поломка:

- `SPLA.Tests.ConversationOrchestratorTests.Repeated_identical_tool_calls_trip_the_loop_guard`
- `SPLA.Tests.MultiProjectProtocolTests.Negotiate_login_issues_cookie_then_ws_authenticates_and_routes_to_user_area`

> **Проверка каждой задачи считается пройденной, если: сборка 0 ошибок, и падают ровно эти два
> теста и никакие другие.** Появился третий — ты что-то сломал. Стало меньше двух — тоже доложи, это
> означает, что базовая линия сдвинулась и план надо пересчитать.

Тестовый проект в решении один: `tests/SPLA.Tests`.

---

## Часть I. Подготовка — поведение не меняется

Три независимые задачи. Порядок между ними безразличен.

### Задача 1. Ось «привязан к разговору» — сделано 2026-08-05

**Зачем.** У инструмента есть три оси метаданных (`ToolScope`, `ToolEffect`, `ToolRisk`), но нет
ответа на вопрос «осмысленен ли этот вызов вне разговора конкретной головы». Нужна четвёртая,
отдельная — вешать на `ToolScope` нельзя, он несущий для модели разрешений.

**Что сделать.**

1. В `ToolFunctionDefinition` добавить `public bool ConversationBound { get; set; }` со значением по
   умолчанию `false` и XML-комментарием: свойство инструмента, а не решение о выдаче; `true`
   означает, что вызов управляет разговором самой головы и вне его бессмыслен.
2. Проставить `ConversationBound = true` ровно у этих инструментов:
   `mark_set`, `mark_rollback`, `checkpoint_save`, `context_rollback`, `agent_get_context`,
   `agent_clarify`, `image_view`, `toolset_activate`, `skill_activate`, `skill_deactivate`.

> **Расхождение, найденное при исполнении (2026-08-05).** Инструментов оказалось **11, а не 10**:
> `ToolSetActivateTool.cs` содержит два класса — `toolset_activate` и `toolset_deactivate`, а
> инвентаризация к плану шла по файлам. `toolset_deactivate` привязан к разговору по той же причине,
> что и симметричный ему `skill_deactivate` (уже был в списке), поэтому размечен вместе с
> остальными. Ожидаемое число в проверке ниже исправлено на 11.

**Можно трогать:** `src/core/SPLA.Domain/Models/ToolDefinition.cs`; перечисленные `*Tool.cs` в
`src/core/SPLA.MCP.Core/Tools/` и `src/agent/SPLA.MCP.BasicTools/System/GetContextTool.cs`.

**Нельзя трогать:** `PermissionEnums.cs`, `PermissionManager.cs`, `McpHost.cs`.

**Инвариант.** Ни одна строка кода нового свойства не читает. Поведение системы тождественно.

**Проверка.** Базовая линия. Плюс: `grep -rc "ConversationBound = true"` по `src/` даёт **11**.

---

### Задача 2. Под-агент наследует песочницу родителя — сделано 2026-08-05

**Зачем.** `SpawnedAgentRunner` создаёт сессию под-агента без песочницы, из-за чего конструктор
`AgentSession` подставляет `PassthroughSandbox.Default`. Под-агент **не наследует песочницу чата,
который его породил** — то есть спавн является выходом из неё. Локально это незаметно (там и так
passthrough), но дефект настоящий.

**Что сделать.** При создании `AgentSession` в `SpawnedAgentRunner` передавать песочницу
родительской сессии (`AgentSessionScope.Current?.Sandbox`), сохранив нынешнее умолчание, когда
родителя нет.

**Можно трогать:** `src/agent/SPLA.Agent/SpawnedAgentRunner.cs`.

**Нельзя трогать:** `AgentSession` / `AgentSessionScope`, инструменты спавна.

**Инвариант.** Остальная изоляция под-агента не меняется: своя KV, свой `CheckpointManager`, своя
`SkillSession` — как было. Наследуется **только** песочница.

**Проверка.** Базовая линия.

---

### Задача 3. Предел глубины спавна — сделано 2026-08-05

**Зачем.** Под-агент ходит через тот же `IToolHost` и может вызвать `agent_spawn` снова. Предела
глубины нет; `EnableLoopGuard` ловит петли вызовов инструментов, но не рекурсию агентов.

**Что сделать.** Счётчик глубины, увеличиваемый на входе в `RunSkillAsync` и уменьшаемый на выходе;
при превышении — вернуть ошибку текстом, не бросать исключение. **Предел: 3.** Значение выбрано
заведомо выше всего, что встречается сегодня, — задача не должна менять наблюдаемое поведение.

**Можно трогать:** `src/agent/SPLA.Agent/SpawnedAgentRunner.cs`.

**Нельзя трогать:** `AgentSpawnTool.cs`, `AgentSpawnBatchTool.cs`.

**Инвариант.** При глубине 1 и 2 поведение тождественно нынешнему.

**Проверка.** Базовая линия.

---

## Часть II. Извлечения — поведение сохраняется

Строго по порядку: 5 опирается на 4, 6 опирается на 5.

### Задача 4. Вердикт как чистая функция с причиной

**Зачем.** Сейчас решение о допуске и его применение слиты внутри `McpHost.ExecuteToolAsync`.
Разделение нужно, чтобы один и тот же вердикт можно было вычислить в двух местах: в голове —
предварительно (узнать, надо ли спрашивать человека), и в теле — при исполнении (отказать).
Дублируется вычисление, а не логика; вычисление чистое и дешёвое.

**Что сделать.**

1. Ввести тип вердикта: исход (`Allow` / `Deny` / `Ask`) **плюс** человекочитаемая причина и
   категория, по которой вердикт получен.
2. `IPermissionManager.CheckPermission` возвращает этот тип вместо голого `PermissionResult`.
   Логика решения переносится **без изменений** — ветки режимов, проектные переопределения,
   запомненные разрешения, обход для `ToolScope.Agent` остаются ровно теми же.
3. Места вызова приводятся к новому типу, читая из него только исход.

**Можно трогать:** `src/core/SPLA.MCP.Core/Permissions/PermissionManager.cs`,
`src/core/SPLA.Domain/Models/PermissionEnums.cs` (добавление типа), `McpHost.cs` (только места
вызова).

**Нельзя трогать:** любые `*Tool.cs`, `ConversationOrchestrator.cs`.

**Инвариант.** Для любых входных данных исход тот же, что был. Причина пока нигде не читается.

**Проверка.** Базовая линия.

---

### Задача 5. `ToolCallContext` — третье время жизни

**Зачем.** Времён жизни сейчас два: проект (`AgentRuntime`: хост, права, тулсеты, плагины) и чат
(`AgentSession`: KV, блобы, метки, скиллы). Третьего — **вызова** — нет: кто вызвал, с какими
правами, куда слать прогресс, какова глубина спавна, выводится из окружения через `AsyncLocal`.

> ### ⚠ Главное правило этой задачи
> **Контекст становится источником, ambient остаётся доступом.**
> `ExecuteToolAsync` принимает контекст и **сам открывает из него привычные `AsyncLocal`-области**.
> Инструменты продолжают читать `AgentSessionScope.Current` и не замечают ничего.
> **Ни один из ~110 файлов `*Tool.cs` в этой задаче не правится.** Если ты правишь инструмент —
> ты делаешь не эту задачу; остановись и доложи.

**Что сделать.**

1. Тип `ToolCallContext`: агентская сессия, личность вызывающего, режим, глубина спавна,
   маркер отмены. Значения по умолчанию — те же, что берутся из окружения сейчас.
2. `IToolHost.ExecuteToolAsync` принимает контекст **необязательным** параметром; при его
   отсутствии собирает из нынешних ambient-источников (полная обратная совместимость).
3. Внутри метода из контекста открываются области — так, как они открываются сегодня.
4. Оба места вызова (`ConversationOrchestrator.cs:217` и `ScriptContext.cs:63`) передают контекст
   явно. У `ScriptContext` контекст наследуется от текущего с увеличенной глубиной.

**Можно трогать:** `src/core/SPLA.Domain/Interfaces/IToolHost.cs`, `McpHost.cs`,
`src/agent/SPLA.Agent/ConversationOrchestrator.cs`,
`src/plugins/SPLA.Plugins.Roslyn/ScriptContext.cs` и `RoslynPlugin.cs` (реализация `IToolHost`),
новый файл под `ToolCallContext`.

**Нельзя трогать:** любые `*Tool.cs`; `AgentSessionScope`, `ClarifyScope`, `ProgressScope`,
`ToolHostScope` — механика областей не меняется, меняется только то, **откуда берутся значения**.

**Инвариант.** Десять параллельных чатов продолжают не сталкиваться. `ctx.Run` из скрипта
по-прежнему попадает в те же области, что и раньше.

**Проверка.** Базовая линия. Плюс: ни один файл с именем `*Tool.cs` не должен появиться в
`git diff --name-only`.

---

### Задача 6. Конвейер вместо восьми зашитых забот

**Зачем.** `McpHost.ExecuteToolAsync` уже представляет собой конвейер, развёрнутый руками в один
метод: доступность плагина → поднятость тулсета → телеметрия → разрешение → область хоста → узел
прогресса → исполнение → учёт результата. Каждая забота со своим ранним возвратом.

Оформить их звеньями нужно, чтобы появилось **место**, куда класть предел параллельности, проверку
владения ресурсом, аудит и (позже) врезку песочницы. Сейчас такого места нет.

**Образец — уже принятый в этом репозитории:** `SPLA.Domain/Llm/ILlmMiddleware.cs` с перечислением
`LlmPipelineStage`, где **порядок задаётся типом стадии, а не порядком регистрации**, и часть стадий
закрыта для плагинов. Делать по этому же шаблону: это не новая абстракция, а второй её экземпляр.

**Что сделать.** Превратить перечисленные восемь забот в упорядоченные звенья. **Ни одной новой
заботы не добавлять** — ни повторов, ни кеша, ни таймаута, ни квот. Только то, что уже есть.

> ### ⚠ Два требования, нарушение которых даёт трудноуловимые ошибки
> **1. Порядок содержателен.** Гейт тулсета обязан идти **до** проверки разрешений (нет смысла
> оценивать права у инструмента, который вообще не раскрыт), а узел прогресса — открываться
> **после** решения (иначе в дереве появятся узлы у несостоявшихся вызовов).
>
> **2. Конвейер обязан быть повторно входимым.** `ctx.Run` заходит в него **изнутри уже идущего
> вызова**. Сегодня это учтено: `ProgressScope.BeginNode` намеренно вкладывает узлы. Любое звено,
> ведущее счёт или открывающее область, должно пережить вложенный вход, иначе телеметрия начнёт
> двоить, а дерево прогресса поедет.

**Можно трогать:** `McpHost.cs`, новые файлы конвейера в `src/core/SPLA.MCP.Core/`.

**Нельзя трогать:** любые `*Tool.cs`; `ILlmMiddleware.cs` и конвейер LLM (он образец, а не предмет
правки).

**Инвариант.** Наблюдаемое поведение тождественно: те же отказы, те же тексты ошибок, то же дерево
прогресса, та же телеметрия.

**Проверка.** Базовая линия.

---

## Часть III. Требует решения владельца — не начинать без ответа

### ❓ Решение 1. `ToolResult` вместо `string`

Результат инструмента сейчас `string`. Три независимых мотива требуют типизации (состояние +
содержимое):

- конвейер не отличает отказ от текста, начинающегося со слова «Error»;
- картинке негде ехать наружу (сейчас `browser_screenshot` возвращает текст, а PNG кладёт в очередь
  картинок чата);
- структурированному отказу негде объявить себя отказом.

Цена: затрагивает `ConversationOrchestrator` и все места, где результат складывается в сообщение.

**Пока решение не принято, задачи 7 и 8 не начинать.**

### Задача 7. `ToolResult` *(после решения 1)*
### Задача 8. Говорящий отказ *(после задачи 7)*

Исход `Ask` без приложенного человека сейчас даёт жёсткую ошибку «requires user confirmation, but no
permission handler is attached» — то есть **безголовый прогон на таком инструменте просто падает**.
Заменяется структурированным отказом с причиной и указанием, как повторить вызов осознанно.
Образец уже есть в репозитории: `sql_execute` с анализом безопасности и параметром `confirmed`.

---

## Часть IV. Внешняя голова — после отдельных решений

Не начинать, пока не отвечены пять вопросов из раздела 10 идеи (своё тело или общее; продуктовая
поверхность или личное удобство; личность; сессия; переводить ли родную голову на провод).

Крупными штрихами: профиль выдачи поверх `ToolSetRegistry` → транспортно-независимый контракт
раздачи + MCP-адаптер + stdio-шим к именованному каналу (закрытый по умолчанию) → личность и
политика секретов, открывающие построенное → проекция блоб-хендлов в ресурсы → elicitation
(`auto`/`off`/`force` + таймаут обратно в говорящий отказ).

**Первый экспортируемый инструмент разумнее всего `agent_spawn`**: один, крупный, курируемый
скиллами, приносит собственную сессию. До его отдачи наружу обязательны задачи 2 и 3 и зажим режима
сверху политикой (сейчас режим выбирает вызывающий).
