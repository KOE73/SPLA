id: test
version: 1.0.0
type: dll
entry_point: SPLA.Plugins.Test.dll
default_prompt: |
  You have access to the 'test' plugin. 
  Test plugin tools are available for plugin-system diagnostics.

  Tools:
    test_ping_host — returns a simple pong response for plugin loading checks.
