# tests

Пусто. Здесь `SplaAtlas.Tests`.

Обязательное покрытие — `SplaAtlas.Model` (круговой рейс чтение → запись без
потерь) и `SplaAtlas.Sync` (долговечность id при переименовании, `status: gone`
вместо удаления, неприкосновенность записей с `origin: manual`).

Отдельный тест, который нельзя пропустить: **конвертер переносит ручную
раскладку один в один** — координаты, размеры и `styleId` каждого узла и каждой
зоны из `docs/diagrams/model-core-full.json`. Сверять числами, а не глазами.
