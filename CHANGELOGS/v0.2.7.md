# v0.2.7

## Summary

## Breaking: resource tools are a capability

The six `resource_*` tools and their prompt block are now the `core.resources` capability, gated by a
session's capability list like skills and working memory, roles included. The `agent.unified_resources`
flag is gone — a manifest that set it must list the capability instead.

## A role governs its own session, from the moment a chat starts

A role's capabilities and tool sets now decide what its own session is offered, call by call, and the
prompt is composed from the same settings, so a role's text and its tools cannot disagree. A chat can
be started as a role from the web ("+ New" gained a role menu), from the protocol, and from the CLI
(`--role` on `chat run` and `chat open`); an unknown role is refused before anything is created.

## AGENTS.md per folder, and `/compact`

AGENTS.md is no longer a single root file. `agent.agents_md` (`inject` by default, or `ignore`) is
cascaded machine → project → role. A write under a folder with its own AGENTS.md is refused until that
folder's rules have been loaded into the prompt, so an agent cannot edit code without having read the
rules that govern it.

`/compact` folds a chat's older history into a summary — from the web composer, the protocol or the
CLI. Nothing is deleted: the history stays on disk, and rolling back across the compaction works.

## Pictures in the context, and in the chat

Pictures are the expensive part of a context, so they got controls. `agent.tool_images: last` keeps
only the newest tool picture; `image_view` and `resource_read` take `keep=once` (gone after the next
picture) or `keep=pinned` (a standing reference that survives later pictures and compaction). Android
keeps only its newest screenshot. Attached images carry a name the model can be told, so several
pictures in one message can be referred to apart. In the web client, tool images now show in the chat
and any image opens in a viewer.

## Tool calls that survive the transport and the context

String-typed arguments are read as the type the tool's schema declares: some serving stacks deliver
every value as text, and `"290"` for `290` was failing calls the model had got right. A tool result is
budgeted against the context that is left, and ssh/docker output is read as a screen instead of a tape.
Rate-limited requests retry with backoff and are paced per connection. A chat's file is saved after
every tool result, so a cancelled or crashed turn no longer loses the calls it had already made.

## Plugins bring their own panels

A plugin can now contribute a dock panel (`web_panel_entry`); Browser Lab moved out of the shell into
the browser plugin's own bundle. An Android plugin drives a device through scrcpy and adb.

## The geometry markup plugin — experimental, not yet successful

**The geometry plugin is experimental and is shipped as-is. In practice it has not worked well yet.**
It is a workspace for marking up an image with boxes and points by looking at a rendered result and
correcting it: coloured box sides, pixel rulers along each edge, moving a side by naming its colour,
zoom and deskew, grids, and a second way to place a box by answering which numbered probes lie on the
object. It has its own skill and a dock panel showing the renders converge. In live runs on local
models the markup did not reach usable accuracy; the tools, their arguments and their replies are
expected to change.

## Also in this release

A connection model can be marked as the project default, and the settings editor no longer deletes a
connection that two layers declare under the same id. `spla chat run --title` names each chat, and
chats it starts are marked in the chat list. `--help` answers before the CLI loads a project, and the
MCP handshake names the project. SQL keeps boolean plugin settings across a save and reads
schema-qualified and bracketed MSSQL names. Duplicating a chat no longer round-trips through YAML, a
chat's live answer survives a log rebuild, tool cards use the full column width, the debug panel
refreshes as a dock panel, and dropped images with no MIME type are accepted.

## Changes

- A tool result is budgeted against the context that is left, and ssh/docker output is read as a screen.
- A rate-limited request is retried with backoff, and requests are paced per connection.
- Every attached image carries a name the model can be told.
- Duplicating a chat copies it from memory instead of round-tripping it through YAML.
- `spla mcp --help` answers directly, and the MCP handshake names the project.
- A tool card and a chat bubble use the full width of the column.
- A chat's live answer stays visible when its log is rebuilt.
- The DPI call on Windows now hits the vtable slot it was always meant for.
- A connection model can be the project default, and its picker label is a valid CLI model name.
- The settings editor picks a model's default without deleting a connection two layers happen to share an id with.
- `--help` and `help all` answer before the CLI bootstraps a project.
- A role's capabilities and tool sets govern its own session.
- Chats started by `spla chat run` are marked in the chat list, and the list row is tidier.
- Chats and spawned runs publish one event stream, and every window subscribes to it.
- Breaking: resource tools are the `core.resources` capability; the `agent.unified_resources` flag is gone.
- A chat can start as a role from the web, the protocol and the CLI.
- `spla chat run --title` names each cell's chat.
- AGENTS.md is read per folder.
- `/compact` folds a chat's history into a summary without losing it.
- SQL: boolean plugin settings survive a save, and `sql_schema` reads schema-qualified and bracketed MSSQL names.
- An Android plugin drives a device through scrcpy and adb.
- A geometry plugin marks up an image with boxes and points — experimental.
- `agent.tool_images: last` keeps only the newest tool picture in the context.
- A plugin can contribute its own dock panel (`web_panel_entry`).
- A chat's file is saved after every tool result, not only at the end of a turn.
- The debug panel refreshes as a dock panel, and its kv column is labelled "zone".
- A picture read by `image_view` or `resource_read` can be kept for one step, or pinned as a reference.
- Android keeps only the newest screenshot in the context.
- Tool images show in the chat, and any image opens in a viewer.
- Geometry: a box can be placed by answering which numbered probes lie on the object
- String-typed tool arguments are read as the type the tool's schema declares.
- The role menu button shows from connect, and dropped images with no MIME type are accepted.

## Log

## 2026-09-09

- **A tool result is budgeted against the context that is left, and ssh/docker output is read as a
  screen.** A result that would not fit is kept out of the context instead of silently crowding the
  conversation; a terminal's output arrives as its current screen rather than as a scrolling tape.
- **A rate-limited request is retried with backoff, and requests are paced per connection.**
- **Every attached image carries a name the model can be told.** The caption travels as a text block
  just before the picture, so several images in one message can be told apart by name.
- **Duplicating a chat copies it from memory instead of round-tripping it through YAML.**
- **`spla mcp --help` answers directly, and the MCP handshake names the project.**
- **A tool card and a chat bubble use the full width of the column.**
- **A chat's live answer stays visible when its log is rebuilt.**
- **The DPI call on Windows now hits the vtable slot it was always meant for.**
- **A connection model can be the project default, and its picker label is a valid CLI model name.**
  Set `default: true` on one model in a configuration layer; the most specific layer that names a
  default wins, while a layer with two defaults is rejected with their ids. New and spawned chats,
  role fallbacks and batch runs all use the resolved default. The status bar shows the qualified
  connection/model label and can copy that exact value for `spla chat run --model`; the settings
  editor preserves the flag without exposing another control.

## 2026-09-10

- **The settings editor picks a model's default without deleting a connection two layers happen to
  share an id with.** Copying a project directory around commonly leaves the same connection id
  (typically `default`) declared in both the personal `connections.yaml` and the project manifest;
  resolution correctly collapses that to one live connection, but the editor was fed the collapsed
  view, so it could not see the shadowed entry and pressing Save rewrote that entry's own layer
  file without it — silently deleting a person's connection. The editor now sees every layer's
  declarations, marks the one a turn will not use as "shadowed", and saves each layer back to
  itself. A checkbox on the model row sets the default (one per scope; the panel clears the others
  in that scope), shown as a star with the model's name on the connection's own collapsed row and
  in the chat's model picker, for reference.
- **`--help` and `help all` answer before the CLI bootstraps a project.**
- **A role's capabilities and tool sets govern its own session.** Every capability's tools are
  registered once, and the host decides per call from the session's settings — the role's, or the
  project's outside one. The prompt is composed from the same settings, so a role's text and its
  tools agree. Spawned runs carry their role's settings into the session.
- **Chats started by `spla chat run` are marked in the chat list, and the list row is tidier.** A
  "chat run" badge sits under the title; row actions stay hidden until hover.

## 2026-09-11

- **Chats and spawned runs publish one event stream, and every window subscribes to it.** Opening a
  chat in the middle of a turn returns a snapshot with its sequence number instead of a partial view.
- **Breaking: resource tools are the `core.resources` capability; the `agent.unified_resources` flag
  is gone.** The six `resource_*` tools and their prompt block are gated by a session's capability
  list, roles included. A manifest that still sets `unified_resources` must list the capability
  instead.
- **A chat can start as a role from the web, the protocol and the CLI.** `chat.new` accepts `role`,
  the "+ New" button gains a role menu, and `spla chat run` / `chat open` take `--role`. An unknown
  role is refused before anything is created.
- **`spla chat run --title` names each cell's chat**, with the same placeholders as `--out-name`.
- **AGENTS.md is read per folder.** `agent.agents_md` (`inject` | `ignore`, default `inject`) is
  cascaded machine → project → role and editable in the agent and role settings. The root AGENTS.md
  is injected into the prompt; a write under a folder with its own AGENTS.md is refused until that
  folder's rules have been loaded, after which they appear as a scoped block in the prompt.
- **`/compact` folds a chat's history into a summary without losing it.** Available from the web
  composer, the protocol (`chat.compact`) and the CLI; older messages stay on disk and are rolled back
  correctly across the compaction boundary. Refused mid-turn.
- **SQL: boolean plugin settings survive a save, and `sql_schema` reads schema-qualified and
  bracketed MSSQL names.**
- **An Android plugin drives a device through scrcpy and adb.**

## 2026-09-14

- **A geometry plugin marks up an image with boxes and points — experimental.** `geom_open`,
  `geom_box`, `geom_point`, `geom_view`, `geom_accept`, `geom_result`: place something roughly, look
  at the rendered result, correct it, and read the finished markup in the original image's
  coordinates. The box being placed has coloured sides, corners, a centre and pixel rulers along each
  edge; a single side can be moved by naming its colour; the view can zoom and deskew; a grid can be
  turned on in the view or inside the box. The loop has its own skill, and the plugin a dock panel
  that shows the renders converging. In live runs on local models the markup did not reach usable
  accuracy, so the plugin stays experimental and is shipped as-is.
- **`agent.tool_images: last` keeps only the newest tool picture in the context.** The default `all`
  keeps today's behaviour; nothing is deleted from the chat either way.
- **A plugin can contribute its own dock panel (`web_panel_entry`).** Browser Lab moved out of the
  shell into the browser plugin's own panel bundle.
- **A chat's file is saved after every tool result, not only at the end of a turn**, so a cancelled
  or crashed turn no longer loses the tool calls it had already made.
- **The debug panel refreshes as a dock panel, and its kv column is labelled "zone".**

## 2026-09-16

- **A picture read by `image_view` or `resource_read` can be kept for one step, or pinned as a
  reference.** `keep=once` is evicted by the next picture whatever `agent.tool_images` says;
  `keep=pinned` is filed under its own name, survives later pictures and compaction, and re-reading it
  replaces it instead of adding a copy.
- **Android keeps only the newest screenshot in the context.**
- **Tool images show in the chat, and any image opens in a viewer.**
- **Geometry: a box can be placed by answering which numbered probes lie on the object** (`geom_probe`),
  with a legibility chart that finds the smallest probe label a model reads, and a settings panel.
  A refused probe answer now comes back with a redrawn picture, and a probe reply no longer describes
  the box it did not draw. Experimental, like the rest of the plugin.
- **String-typed tool arguments are read as the type the tool's schema declares.** Some serving
  stacks deliver every value as text (`"290"` for `290`); the pipeline now converts such values before
  the permission check and the tool see them, while the log keeps what the provider sent.
- **The role menu button shows from connect, and dropped images with no MIME type are accepted.**
